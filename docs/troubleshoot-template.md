<!--
════════════════════════════════════════════════════════════════════════
藍鴨 · 錯誤排除文章 SEO 模板 (troubleshoot)
用途:任何「AI CLI 工具安裝/執行報錯」的頁面都套這個模子。
放在 docs/,不要放 src/content/articles(未填的模板會讓 Astro build 失敗)。
要寫新文章時:複製本檔 → 存成 src/content/articles/<slug>.md → 把 {{...}} 全部填掉 → 刪掉所有 <!-- SEO: --> 註解。
────────────────────────────────────────────────────────────────────────
SLUG(檔名)規則:工具 + 錯誤關鍵字,全小寫連字號。
  例:ollama-hermes-unknown-integration.md
  檔名就是網址,錯誤關鍵字要進檔名。
════════════════════════════════════════════════════════════════════════
-->
---
# SEO: 標題「必須」逐字包含最主要的錯誤字串——那就是關鍵字本身。
# 句型:「『<錯誤字串>』怎麼解?<工具> <情境> 完整排除(Mac / Windows)」
title: "「{{ERROR_STRING}}」怎麼解?{{TOOL}} {{CONTEXT}}報錯完整排除"

# SEO: description = 再貼一次錯誤 + 承諾解法 + 給非技術者的安心話。150 字內、自然語氣。
description: "執行 {{COMMAND}} 跳出「{{ERROR_STRING}}」?這篇給你一句話解法,再分 Mac 與 Windows 一步步帶你修好,每步有截圖。不會寫程式也能自己排除。"

contentType: "troubleshoot"
scene: "安裝與部署"            # 錯誤頁固定用這個(schema enum 內)
difficulty: "入門"             # 受眾是非技術者,一律入門
createdAt: "{{YYYY-MM-DD}}"
verifiedAt: "{{YYYY-MM-DD}}"
compatibleVersion: "{{工具版本,例:ollama 0.x + hermes cloud}}"  # 錯誤頁一定要標版本:版本一改解法就變,這是可信度關鍵
estimatedMinutes: 6
prerequisites: ["{{相關安裝教學的 slug,例:hermes-agent}}"]
# SEO: tags 放「工具名 + 錯誤排除 + 平台」,3–8 個。
tags: ["{{工具}}", "錯誤排除", "安裝", "CLI", "{{平台}}"]
# 卡點回報:把「還是同一個錯 / 出現別的錯」寫成使用者會點的問句
stuckOptions:
  "還是同一個錯": ["我照做了但錯誤沒消失", "我不確定我打的指令對不對"]
  "出現別的錯": ["改跳出另一段紅字", "這步驟找不到對應的畫面"]
---

<!-- SEO: 【第一屏 = featured snippet 誘餌】開頭就給一句話解法。
     搜錯誤的人沒耐心,而 Google 抓精選摘要也最愛「問題→一句解答」這種結構。
     用藍鴨既有的「鴨編快說重點」樣式保持全站一致。 -->
> <img src="/images/dock_head_s.png" alt="鴨編" width="24" style="vertical-align: middle;"> **鴨編快說重點**:會跳「{{ERROR_STRING}}」,通常是因為 {{一句話原因}}。最快解法是 {{一句話解法,例:先更新工具再重下指令}}。下面分 Mac / Windows 帶你做一次。

<!-- SEO: 【原文錯誤區塊】把錯誤「一字不改」貼進 code block。
     這是給 Google 做完全比對用的——這一塊決定你排不排得上。
     底下再列「這頁也能解的變體」,一頁吃下整叢近似查詢。 -->
## 你看到的錯誤長這樣

```
{{把完整的錯誤輸出原封不動貼在這裡,含前後幾行}}
```

如果你搜到的字串跟上面不完全一樣,別擔心,以下這些其實是同一個問題,解法相同:

- `{{變體字串 1}}`
- `{{變體字串 2}}`
- `{{變體字串 3}}`

<!-- SEO: 【為什麼會這樣】1–3 句白話,不要技術長篇。
     目的:建立「他真的懂這個錯」的信任,順便自然帶入相關關鍵字。 -->
## 為什麼會出現這個錯?

{{用兩三句白話解釋成因。例:hermes 這個整合名稱在新版 ollama 改了 / 你的工具版本太舊 / PATH 沒重新載入。避免術語轟炸。}}

<!-- SEO: 【解法】這是主體。分平台寫,因為錯誤查詢常帶 mac / windows / wsl。
     每一步:一句「做什麼」+ 指令(code block)+「你會看到什麼」。
     截圖插在「你會看到什麼」那句後面(見文末截圖規格)。 -->
## 解法 · macOS

1. **{{步驟一在做什麼}}**
   ```
   {{指令}}
   ```
   {{做完你會看到什麼——這裡放截圖}}

2. **{{步驟二}}**
   ```
   {{指令}}
   ```

3. **{{步驟三}}**

## 解法 · Windows(WSL)

<!-- 若這個錯只發生在單一平台,刪掉不相干的那節,標題也拿掉「(Mac / Windows)」。 -->

1. **{{步驟一}}**
   ```
   {{指令}}
   ```

2. **{{步驟二}}**

<!-- SEO: 【驗證】給一個「怎麼確認修好了」——降低跳出率,也是 HowTo schema 的收尾步驟。 -->
## 怎麼確認已經解決?

{{一個能確認成功的動作 + 成功時會看到的畫面/輸出。}}

```
{{成功時的輸出}}
```

<!-- SEO: 【還是不行 = hub-and-spoke 內鏈】
     這一段把權重導向共通主幹頁,也接住沒解掉的人。
     每個 troubleshoot 頁都要往「共通錯誤主幹頁」內鏈——這是整個系列排名滾起來的關鍵。 -->
## 還是沒解決?

- 如果卡在「{{通用錯誤,例:command not found}}」→ 看〔{{共通主幹頁標題}}〕({{/articles/共通頁slug}})
- 如果卡在權限 / `permission denied` → 看〔{{權限主幹頁}}〕
- 還是不行?到[藍鴨小聚討論區]({{討論區連結}})把你的紅字貼上來,鴨編幫你看。

<!-- SEO: 【相關】往「安裝教學」正頁與同工具其他錯誤頁內鏈,建立主題群。 -->
## 相關教學

- {{該工具的安裝正篇}}
- {{同工具的其他錯誤頁}}

<!--
════════════════════════════════════════════════════════════════════════
【結構化資料 FAQ schema — 強烈建議,但不要塞在 markdown 裡】
troubleshoot 頁若掛上 FAQPage / HowTo schema,能吃到 Google 的「常見問題」
複合式結果,CTR 明顯拉高。但 <script> 塞在 .md 可能被 Astro 清掉。
正確做法:在 ArticleLayout.astro 裡,判斷 frontmatter.contentType === 'troubleshoot'
時注入下面這段 JSON-LD(用文章的 title/description/步驟去填)。這一步開一張獨立
CC 交接單做一次,之後所有 troubleshoot 頁自動套用。

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [{
    "@type": "Question",
    "name": "{{ERROR_STRING}} 怎麼解?",
    "acceptedAnswer": { "@type": "Answer", "text": "{{一句話解法}}" }
  }]
}
</script>
════════════════════════════════════════════════════════════════════════
-->
